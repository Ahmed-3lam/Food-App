import 'package:frontend/Helpers/mlib.dart';

class TableReservationPopUp extends StatelessWidget {
  const TableReservationPopUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
